<form method="post" action="{{ path('app_suivi_conge_delete', {'id': suivi_conge.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ suivi_conge.id) }}">
    <button class="btn">Delete</button>
</form>
